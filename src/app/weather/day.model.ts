interface Day {
    name: string;
    temperature: number;
    image?: string;
    date: string; // Add date property to the interface
    condition: string; // Add condition property to the interface
}